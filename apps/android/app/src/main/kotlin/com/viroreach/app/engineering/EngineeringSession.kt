package com.viroreach.app.engineering

import com.viroreach.app.session.SessionManager

/** Engineering harness uses the same singleton as the consumer app. */
typealias EngineeringSession = SessionManager
